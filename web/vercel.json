{
    "builds": [{
        "src": "web/wsgi.py",
        "use": "@vercel/python"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "web/wsgi.py"
    }],
    "rewrites": [
        { "source": "/(.*)", "destination": "/" }
    ]
      
}